# basic-node

Plain Node.js project — dùng để test `harness-kit init` trên project không có TypeScript.

## Test

```bash
# từ root repo
pnpm build
node ../../packages/harness-kit/dist/index.js init
```
