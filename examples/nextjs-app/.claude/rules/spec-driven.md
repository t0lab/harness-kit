# Spec Driven

Before planning or implementing non-trivial work, read `.agents/skills/spec-driven/`.
For features, API changes, refactors, or other scope-sensitive work, write or update the spec in `docs/` first.
If no spec is needed because the change is mechanical, say that explicitly.
