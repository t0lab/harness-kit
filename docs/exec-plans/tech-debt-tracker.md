# Tech Debt Tracker

Tài liệu này theo dõi các vấn đề kỹ thuật đã biết để các agent không phải tái khám phá.

| Item | File | Mức độ | Ghi chú |
|------|------|--------|---------|
| xstate type assertion lặp lại | `packages/harness-kit/src/wizard/index.ts` | Trung bình | Mỗi transition dùng `event as Extract<WizardEvent, { type: 'NEXT' }>` thay vì typegen. XState v5 có hỗ trợ typegen nhưng chưa được kích hoạt — xem dòng 44, 53, 58, 62, 71 |
| `detect-tech.ts` và `detector.ts` phân chia trách nhiệm | `packages/harness-kit/src/wizard/detect-tech.ts`, `packages/harness-kit/src/wizard/detector.ts` | Trung bình | Hai module phát hiện công nghệ tồn tại song song: `detectTechStack()` trong `detect-tech.ts` đọc `package.json`/`requirements.txt` để suy luận tech stack từ dependencies; `detectTooling()` trong `detector.ts` kiểm tra sự hiện diện của file cấu hình (ESLint, Prettier, tsconfig...) dựa trên tech stack đã chọn. Phân chia là có chủ ý nhưng chưa có tài liệu rõ ràng — dễ gây nhầm lẫn khi bảo trì |
| Tech options không có bundle | `packages/harness-kit/src/wizard/tech-options.ts` | Trung bình | `go` và `rust` đã bị xoá khỏi `TECH_OPTIONS` vào 2026-04-15 vì không có bundle `techstack/go` hay `techstack/rust`. Khi thêm lại, cần tạo bundle techstack tương ứng (inherit từ `stack/go` / `stack/rust`) trước. Hiện có validation runtime (`installAllSelectedBundles` warn khi bundle chưa đăng ký), nhưng chưa có compile-time check nào ràng buộc `TechOption.id` với bundle name trong registry |
| `TEMPLATES_DIR` path resolution bằng string check | `packages/harness-kit/src/engine/template-renderer.ts` | Thấp | Dùng `__dir.includes('/dist')` để phân biệt môi trường build và test (dòng 8). Cách này dễ vỡ nếu cấu trúc thư mục thay đổi; nên dùng biến môi trường hoặc config package |
| Error handling trong `renderTemplate` chỉ bắt `ENOENT` | `packages/harness-kit/src/engine/template-renderer.ts` | Thấp | Lỗi compile Handlebars (template syntax sai) sẽ throw ra ngoài mà không có context — không có thông báo lỗi nào chứa tên template. Nên wrap `Handlebars.compile(source)` và `template(context)` với thông tin tên template |
| `WizardEvent` không có `data` trên `ERROR` event trong typegen | `packages/harness-kit/src/wizard/types.ts` | Thấp | `ERROR` event chỉ có `error: Error`. Tuy nhiên trong `index.ts` dòng 145, event được gửi đúng. Không phải lỗi nhưng `ERROR` state chuyển sang `done` mà không lưu error vào context — mất thông tin lỗi |
| `fileExists` được định nghĩa trùng | `packages/harness-kit/src/wizard/detector.ts`, `packages/harness-kit/src/commands/status.ts` | Thấp | Hai file đều khai báo hàm `fileExists` dùng `access` theo cùng pattern. Chưa đủ 3 bản sao để extract (coding.md: ngưỡng DRY là 3), nhưng nên để ý nếu có thêm |
| `filter.ts` không được wire vào production | `packages/harness-kit/src/wizard/filter.ts` | Thấp | `filterOptions(query, options)` chỉ được dùng trong `tests/wizard/filter.test.ts`, không được import bởi bất kỳ file production nào. Logic filter tương tự tồn tại inline trong `tech-stack-select.ts` (hàm `filter` của `AutocompletePrompt`). Cần quyết định: xóa `filter.ts` hoặc thay thế logic inline bằng hàm này |
| `auditHarness` dùng `defaultRole` thay vì role đã cài | `packages/harness-kit/src/commands/status.ts` (dòng 91) | Thấp | `collectEnvVars()` lấy env vars theo `bundle.defaultRole`, không theo role thực tế được dùng khi `harness-kit add <bundle> --role <r>`. Nếu bundle được cài với role khác `defaultRole`, env vars của role đó sẽ không được audit. `harness.json` hiện chỉ lưu bundle name, không lưu role đã chọn — cần mở rộng schema để fix |
