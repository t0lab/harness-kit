# GitHub Actions

When writing, reviewing, or debugging GitHub Actions workflows — syntax, triggers, matrices, runners, reusable workflows, artifacts, caching, secrets, OIDC, deployments, custom actions — consult `.agents/skills/github-actions-docs/` and ground answers in official GitHub docs.
