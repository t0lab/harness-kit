# LangChain

When writing LangChain agents, consult `.agents/skills/langchain-fundamentals/`. **Always use `create_agent()`** with middleware for custom flows, the `@tool` decorator for tools, and standard patterns for human-in-the-loop and error handling. Older agent classes are outdated.
