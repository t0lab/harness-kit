# Vue

When writing or reviewing Vue code, consult `.agents/skills/vue-best-practices/`. Default to Composition API with `<script setup>` and TypeScript unless the project explicitly requires Options API.
