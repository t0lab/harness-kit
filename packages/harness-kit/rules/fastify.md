# Fastify

When building or reviewing a Fastify server — routes, plugins, JSON Schema validation, lifecycle hooks, Pino logging, error handling, auth, CORS, WebSockets, or deployment — consult `.agents/skills/fastify-best-practices/` before implementing.
