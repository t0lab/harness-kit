# LangGraph

When writing any LangGraph code, consult `.agents/skills/langgraph-fundamentals/` — covers `StateGraph`, state schemas, nodes, edges, `Command`, `Send`, `invoke`, streaming, and error handling.
