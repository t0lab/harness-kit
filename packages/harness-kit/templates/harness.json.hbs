{
  "version": "1.0.0",
  "registry": "bundled",
  "techStack": [{{#each selectedTech}}"{{this}}"{{#unless @last}}, {{/unless}}{{/each}}],
  "bundles": [{{#each bundles}}"{{this}}"{{#unless @last}}, {{/unless}}{{/each}}],
  "ide": [{{#each ide}}"{{this}}"{{#unless @last}}, {{/unless}}{{/each}}]
}
